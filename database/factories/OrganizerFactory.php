<?php

namespace Database\Factories;

/**
 * OrganizerFactory intentionally removed: Organizer data now lives on the users table.
 * Keeping this file (empty) avoids breakage if a package or older test references it.
 */

use Illuminate\Database\Eloquent\Factories\Factory;

class OrganizerFactory extends Factory
{
    public function definition(): array
    {
        return [];
    }
}
