<?php

namespace App\Models;

use Illuminate\Database\Eloquent\Model;

/**
 * DEPRECATED: Organizer model moved into the users table schema.
 * This stub remains for backward compatibility and will be removed
 * in a future cleanup when the codebase no longer references it.
 */
class Organizer extends Model
{
    // Intentionally left minimal to avoid accidental DB operations.
    protected $guarded = [];
}
